# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build forex-marketplace`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t forex-marketplace`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system forex-marketplace && \
          adduser --system -G forex-marketplace forex-marketplace

COPY apps/forex-marketplace/dist forex-marketplace/
RUN chown -R forex-marketplace:forex-marketplace .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix forex-marketplace --omit=dev -f install

CMD [ "node", "forex-marketplace" ]
